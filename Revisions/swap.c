#include <stdio.h>
#include <stdlib.h>

/*
    Exercice : echanger les valeurs de deux variables
*/

int main()
{
    int a = 1;
    int b = 2;

    printf("a vaut : %d\n", a);
    printf("b vaut : %d\n", b);

    // Faire l'echange des variables ici

    // Fin de l'echange des variables

    printf("a vaut maintenant: %d\n", a);
    printf("b vaut maintenant: %d\n", b);


    return 0;
}
